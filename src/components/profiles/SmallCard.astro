---
import type { CollectionEntry } from 'astro:content';
interface Props { entry: CollectionEntry<'profiles'> }
const { entry } = Astro.props;
const { data, id } = entry;
---
<a href={`/profiles/${id}`} class="group rounded-2xl p-6 md:p-7 transition">
  <div class="flex items-center gap-4">
    {data.avatar ? (
      <img
        src={data.avatar}
        alt={data.name}
        class="md:h-12 md:w-12 rounded-full object-cover border-black"
        loading="lazy"
      />
    ) : (
      <div class="h-10 w-10 md:h-24 md:w-24 rounded-full bg-slate-200 grid place-items-center text-slate-600 text-2xl md:text-3xl">
        {data.name?.slice(0,1) ?? "?"}
      </div>
    )}
    <div>
      <h3 class="text-lg md:text-xl font-semibold leading-snug group-hover:underline">{data.name}</h3>
    </div>
  </div>  
</a>
